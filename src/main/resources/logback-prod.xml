<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

    <property name="LOG_DIR" value="./bms_logs/" />
    <property name="MAX_HISTORY" value="31" />
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- INFO/WARN/ERROR 寫入單一檔案，每日一檔 -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/bms_app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 每天產生一份歷史壓縮檔 -->
            <fileNamePattern>${LOG_DIR}/bms_app-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 若未來想啟用 DEBUG 寫入獨立檔案，可取消註解下列區塊 -->
    <!--
    <appender name="DEBUG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/bms_debug.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/bms_debug-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>
    -->

    <!-- root 設定：輸出 console 與 app log（DEBUG級別會印，但寫入檔案會依實際 logger 控制） -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="APP_FILE" />
        <!-- <appender-ref ref="DEBUG_FILE" /> -->
    </root>

</configuration>
